<UserControl x:Class="ImageAnnotator.Controls.AnnotationListItemControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
            d:DesignHeight="40" d:DesignWidth="50">
    <StackPanel Orientation="Vertical">
        <!-- What is the data context here? -->
        <!-- Decide if we always need to bind to an IAnnotation -->

        <!-- Override the DataContext use the internal property instead -->
        <Label Content="{Binding AnnotationName, RelativeSource={RelativeSource AncestorType=UserControl}}" />
    </StackPanel>
</UserControl>